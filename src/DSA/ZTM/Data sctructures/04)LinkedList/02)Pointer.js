// ! what is a pointer
// * pointer is a reference to another place in a memory, object, variable anything

// for example 
const object1 = { a: true }
const obj2 = object1;

//  here we created a pointer to object1, so now object1 and obj2 points to same thing in the memory
