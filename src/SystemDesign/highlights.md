The technical terms for collaborative softwares is `groupware systems`
`last write wins` is the term used for the updates that happens when multiple users updates the same thing and the last call happened to the server will get saved. so the change which made by the last person will be saved hence the name.
